#server:
#  port: 9001
spring:
  datasource:
    url: jdbc:mysql://192.168.0.102:3306/microservices
    username: sandeep
    password: sandeep
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#----------------below logging will activate the jpa transactional log
#actuator endpoint managemnet
management:
  endpoint:
    shutdown:
      enabled: true
    beans:
      enabled: true
    env:
      enabled: true


logging:
  charset:
    file: C:\Users\sande\OneDrive\Desktop\log\elk-stack.log
  level:
    org.springframework.orm.jpa: DEBUG
    org.springframework.transaction: DEBUG

